name: CI
on:
    push:
        jobs:
            build:
                runs-on: ubuntu-latest
                steps:
                    - name: Clonacion del codigo
                      uses: actions/checkout@v4